miniwdl==1.12.0
docker==5.0.3
PyVCF3==1.0.3  # For vcf_fileter_script.py - may not be needed if script to be housed in docker container
pysam==0.23.0  # To enable the install of PyVCF3 - may not be needed if script to be housed in docker container